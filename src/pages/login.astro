---
import Layout from '~/layouts/PageLayout.astro';
const metadata = {
  title: 'Login — Your Platform Name',
};
---

<Layout metadata={metadata}>
  <!-- Hero Section -->
  <section class="relative bg-gradient-to-r from-blue-500 to-indigo-600 text-white py-16 px-4 text-center">
    <h1 class="text-4xl font-bold">Welcome Back!</h1>
    <p class="mt-2 text-lg">Log in to continue your journey with us.</p>
  </section>

  <!-- Login Form Section -->
  <section class="flex justify-center items-center min-h-screen bg-gray-50 dark:bg-gray-900">
    <div class="w-full max-w-md bg-white dark:bg-gray-800 p-8 rounded-lg shadow-lg">
      <h2 class="text-2xl font-semibold text-gray-900 dark:text-white text-center">Login</h2>
      <form action="#" method="POST" class="mt-6">
        <div class="mb-4">
          <label for="email" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Email</label>
          <input type="email" id="email" name="email" required class="mt-1 w-full px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300 dark:bg-gray-700 dark:border-gray-600 dark:text-white">
        </div>

        <div class="mb-4">
          <label for="password" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Password</label>
          <input type="password" id="password" name="password" required class="mt-1 w-full px-4 py-2 border rounded-lg focus:ring focus:ring-blue-300 dark:bg-gray-700 dark:border-gray-600 dark:text-white">
        </div>

        <button type="submit" class="w-full px-4 py-2 mt-4 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg shadow-md transition-all">
          Sign In
        </button>

        <p class="mt-4 text-center text-gray-600 dark:text-gray-400">
          Don't have an account? <a href="/signup" class="text-blue-500 hover:underline">Sign up</a>
        </p>
      </form>
    </div>
  </section>
</Layout>
