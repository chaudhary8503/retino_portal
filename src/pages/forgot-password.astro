---
import { useState } from 'preact/hooks';

const handleForgotPassword = (event) => {
  event.preventDefault();
  console.log("Reset Password button clicked!");
  // Here, you can integrate API logic for password reset.
};
---

<head>
  <title>Forgot Password | RetinoPortal</title>
</head>

<section class="min-h-screen flex items-center justify-center bg-gray-100 dark:bg-gray-900">
  <div class="bg-white dark:bg-gray-800 p-8 rounded-lg shadow-md w-full max-w-md">
    <h2 class="text-2xl font-bold text-center text-gray-800 dark:text-white mb-6">Forgot Password?</h2>
    
    <p class="text-gray-600 dark:text-gray-400 text-center mb-4">
      Enter your email, and we’ll send you a link to reset your password.
    </p>

    <form onSubmit={handleForgotPassword} class="space-y-4">
      <!-- Email -->
      <div>
        <label for="email" class="block text-gray-700 dark:text-gray-300">Email</label>
        <input 
          type="email" 
          id="email" 
          name="email" 
          required 
          class="w-full px-4 py-2 border rounded-md focus:ring focus:ring-blue-300 dark:bg-gray-700 dark:text-white"
          placeholder="Enter your email"
        />
      </div>

      <!-- Reset Password Button -->
      <button 
        type="submit" 
        class="w-full bg-blue-500 text-white font-bold py-2 rounded-md hover:bg-blue-600 transition"
      >
        Reset Password
      </button>
    </form>

    <!-- Back to Login Link -->
    <p class="text-center text-gray-600 dark:text-gray-400 mt-4">
      Remember your password? <a href="/login" class="text-blue-500 hover:underline">Login</a>
    </p>
  </div>
</section>
