---
import type { Content as Props } from '~/types';
import Headline from '../ui/Headline.astro';
import WidgetWrapper from '../ui/WidgetWrapper.astro';
import Image from '~/components/common/Image.astro';
import Button from '~/components/ui/Button.astro';
import ItemGrid from '../ui/ItemGrid.astro';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline,
  content = await Astro.slots.render('content'),
  callToAction,
  items = [],
  columns,
  image1 = await Astro.slots.render('imj1'),  // Reference for content 1 image
  image2 = await Astro.slots.render('imj2'),  // Reference for content 2 image
  isReversed = false,
  isAfterContent = false,

  id,
  isDark = false,
  classes = {},
  bg = await Astro.slots.render('bg'),
} = Astro.props;
---

<WidgetWrapper
  id={id}
  isDark={isDark}
  containerClass={`max-w-7xl mx-auto ${isAfterContent ? 'pt-0 md:pt-0 lg:pt-0' : ''} ${classes?.container ?? ''}`}
  bg={bg}
>
  <Headline
    title={title}
    subtitle={subtitle}
    tagline={tagline}
    classes={{
      container: 'max-w-xl sm:mx-auto lg:max-w-2xl',
      title: 'text-4xl md:text-5xl font-bold tracking-tighter mb-4 font-heading',
      subtitle: 'max-w-3xl mx-auto sm:text-center text-xl text-muted dark:text-slate-400',
    }}
  />

  <div class="mx-auto max-w-7xl p-4 md:px-8">
    <!-- Content 1: Reduced bullet points to 2 -->
    <div class="md:flex md:gap-16">
      <div class="md:basis-1/2 self-center">
        {content && <div class="mb-12 text-lg dark:text-slate-400" set:html={content} />}

        <ItemGrid
          items={items.slice(0, 2)}  <!-- Reduced bullet points to 2 -->
          columns={columns}
          defaultIcon="" <!-- Removed the default icon -->
          classes={{
            container: `gap-y-4 md:gap-y-8`,
            panel: 'max-w-none',
            title: 'text-lg font-medium leading-6 dark:text-white ml-2 rtl:ml-0 rtl:mr-2',
            description: 'text-muted dark:text-slate-400 ml-2 rtl:ml-0 rtl:mr-2',
            action: 'text-lg font-medium leading-6 dark:text-white ml-2 rtl:ml-0 rtl:mr-2',
          }}
        />
      </div>

      <!-- Content 1 Image -->
      <div aria-hidden="true" class="mt-10 md:mt-0 md:basis-1/2">
        {image1 && (
          <div class="relative m-auto max-w-4xl">
            {typeof image1 === 'string' ? (
              <Fragment set:html={image1} />
            ) : (
              <Image
                class="mx-auto w-full rounded-lg bg-gray-500 shadow-lg"
                width={500}
                height={500}
                widths={[400, 768]}
                sizes="(max-width: 768px) 100vw, 432px"
                layout="responsive"
                {...image1}
              />
            )}
          </div>
        )}
      </div>
    </div>

    <!-- Content 2: Image to the right and bullet points to the left -->
    <div class="md:flex md:gap-16 mt-16">
      <!-- Content 2 Image on the right -->
      <div aria-hidden="true" class="mt-10 md:mt-0 md:basis-1/2">
        {image2 && (
          <div class="relative m-auto max-w-4xl">
            {typeof image2 === 'string' ? (
              <Fragment set:html={image2} />
            ) : (
              <Image
                class="mx-auto w-full rounded-lg bg-gray-500 shadow-lg"
                width={500}
                height={500}
                widths={[400, 768]}
                sizes="(max-width: 768px) 100vw, 432px"
                layout="responsive"
                {...image2}
              />
            )}
          </div>
        )}
      </div>

      <div class="md:basis-1/2 self-center">
        <ItemGrid
          items={items.slice(2)}  <!-- Bullet points for content 2, starting from 3rd item -->
          columns={columns}
          defaultIcon="" <!-- Removed the default icon -->
          classes={{
            container: `gap-y-4 md:gap-y-8`,
            panel: 'max-w-none',
            title: 'text-lg font-medium leading-6 dark:text-white ml-2 rtl:ml-0 rtl:mr-2',
            description: 'text-muted dark:text-slate-400 ml-2 rtl:ml-0 rtl:mr-2',
            action: 'text-lg font-medium leading-6 dark:text-white ml-2 rtl:ml-0 rtl:mr-2',
          }}
        />
      </div>
    </div>
  </div>
</WidgetWrapper>

<!-- Content 4 removed and placed outside the WidgetWrapper -->
<!-- You can add any content related to "content 4" here, outside the WidgetWrapper -->
